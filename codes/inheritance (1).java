  static class Cardio extends Workout {
        Cardio(int id, int week, int day, LocalDate date, String intensity, String goal) {
            super(id, week, day, date, intensity, goal);
        }
    }

    static class Strength extends Workout {
        Strength(int id, int week, int day, LocalDate date, String intensity, String goal) {
            super(id, week, day, date, intensity, goal);
        }
    }

    static class Flexibility extends Workout {
        Flexibility(int id, int week, int day, LocalDate date, String intensity, String goal) {
            super(id, week, day, date, intensity, goal);
        }
    }
}